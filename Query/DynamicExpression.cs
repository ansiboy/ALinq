using System;
using System.Collections.Generic;
using System.Linq.Expressions;
using System.Linq;

namespace ALinq.Dynamic
{
    internal static class DynamicExpression
    {
        //public static Expression Parse(Type resultType, string expression, params object[] values)
        //{
        //    var parser = new ExpressionParser(expression, null, values); //new ExpressionParser(null, expression, values);
        //    return parser.Parse(resultType, null);
        //}

        

        //public static Type CreateClass(params DynamicProperty[] properties)
        //{
        //    return ClassFactory.Instance.GetDynamicClass(properties, typeof(DynamicObject));
        //}

        //internal static Type CreateClass(IEnumerable<DynamicProperty> properties, Type baseType)
        //{
        //    return ClassFactory.Instance.GetDynamicClass(properties, baseType);
        //}
    }
}